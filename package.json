{
  "name": "si-instagram-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"python3 backend/app.py\" \"npm run dev --prefix frontend\"",
    "install:frontend": "npm install --prefix frontend",
    "install:backend": "pip3 install -r backend/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
